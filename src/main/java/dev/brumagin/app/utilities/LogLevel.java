package dev.brumagin.app.utilities;

/**
 * Log levels
 * Info - general information
 * Debug - items that should be debugged if they occur
 * Warning - items that represent a problematic program state
 * Error - items that will crash the program / need immediate attention
 */
public enum LogLevel {
    INFO,
    DEBUG,
    WARNING,
    ERROR
}
